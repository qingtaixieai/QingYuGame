import {chromium} from 'file:///C:/Users/lenovo/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import {readFileSync,writeFileSync} from 'node:fs';import assert from 'node:assert/strict';
const password=readFileSync('../.local/admin-access.txt','utf8').split(/\r?\n/).find(l=>l.startsWith('管理员密码：')).slice('管理员密码：'.length);
const browser=await chromium.launch({channel:'msedge',headless:true}),page=await browser.newPage({viewport:{width:1440,height:900}}),errors=[];
page.on('pageerror',e=>errors.push(e.message));
try{await page.goto('https://qingtaixieai.com/',{waitUntil:'networkidle'});await page.getByRole('textbox',{name:'用户名',exact:true}).fill('admin');await page.getByRole('textbox',{name:'密码',exact:true}).fill(password);await page.getByRole('button',{name:'进入青屿大陆',exact:true}).click();await page.locator('canvas').waitFor();await page.getByRole('button',{name:'背包',exact:true}).click();await page.locator('.inventory-list .place-row').first().waitFor();assert.equal(await page.locator('.inventory-list .place-row').count(),2);await page.screenshot({path:'../.local/resources-production.png'});await page.getByRole('button',{name:'管理后台',exact:true}).click();await page.getByRole('button',{name:'查看背包',exact:true}).first().click();await page.locator('.admin-bag').waitFor();assert.equal(await page.locator('.admin-bag .bag-line').count(),2);await page.screenshot({path:'../.local/resources-admin-production.png'});assert.deepEqual(errors,[]);console.log('PASS production canvas, styled inventory, admin inventory and no runtime errors');writeFileSync('../.local/resources-browser-report.json',JSON.stringify({passed:true,errors,date:new Date().toISOString()},null,2));}finally{await browser.close();}
